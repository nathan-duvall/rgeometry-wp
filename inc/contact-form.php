<?php
/**
 * Deprecated in v0.5. Contact form handling moved to a shortcode-based
 * approach (expected: Gravity Forms) so the theme doesn't ship its own form
 * handler.
 *
 * File retained so existing require_once paths don't fatal if referenced
 * anywhere in the wild. No runtime behavior.
 *
 * @package RGeometry
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
